# INSTRUCTIONS

Build, launch, and development guidelines for the **Autonomous Quadcopter
Framework** (ROS 2 Humble + PX4 + NVIDIA Jetson).

---

## Table of Contents

1. [System Overview](#1-system-overview)
2. [Prerequisites](#2-prerequisites)
3. [Repository Layout](#3-repository-layout)
4. [Workspace Setup](#4-workspace-setup)
5. [Building the Workspace](#5-building-the-workspace)
6. [Running the System](#6-running-the-system)
7. [Testing](#7-testing)
8. [Development Workflow](#8-development-workflow)
9. [Troubleshooting](#9-troubleshooting)
10. [Safety & Operational Guidelines](#10-safety--operational-guidelines)

---

## 1. System Overview

This repository implements an autonomous aerial navigation stack for the
**Holybro X500 V2** airframe.

| Layer           | Technology                                              |
| --------------- | ------------------------------------------------------- |
| OS              | Ubuntu 22.04 LTS / JetPack 5.x (Jetson)                 |
| Middleware      | ROS 2 Humble Hawksbill                                  |
| Flight stack    | PX4 Autopilot v1.14+                                    |
| Bridge          | `px4_ros_com` + `px4_msgs` (uXRCE-DDS / MicroXRCE-Agent) |
| Autonomy        | `quadcopter_bringup` (planning, VIO, detection nodes)   |
| Simulation      | Gazebo Ignition (SITL)                                  |

The companion computer (Jetson) talks to the Pixhawk 6C over UART
(`/dev/ttyTHS1`) through the MicroXRCE-DDS agent; PX4 uORB topics are exposed on
the ROS 2 graph via `px4_msgs`.

---

## 2. Prerequisites

### 2.1 Host Requirements

- **Ubuntu 22.04 LTS (jammy)**, 64-bit.
- **ROS 2 Humble** (desktop or ros-base).
- Python 3.10+, `colcon`, `rosdep`.

### 2.2 Install ROS 2 Humble

Follow the official
[ROS 2 Humble installation guide](https://docs.ros.org/en/humble/Installation.html).
Then install the build toolchain:

```bash
sudo apt install -y python3-colcon-common-extensions python3-rosdep
sudo rosdep init
rosdep update
```

### 2.3 Optional / Peripheral Tools

| Tool               | Purpose                              | Install                                    |
| ------------------ | ------------------------------------ | ------------------------------------------ |
| PX4 Autopilot      | Firmware (SITL or hardware flashing) | [PX4 Dev Guide](https://docs.px4.io/main/en/dev_setup/building_px4.html) |
| Gazebo Ignition   | SITL simulation environment          | Installed via the PX4 toolchain            |
| MicroXRCE-Agent    | uXRCE-DDS agent (companion side)     | `sudo apt install ros-humble-micro-ros-agent` or build from source |
| QGroundControl     | Ground control station               | [QGC Downloads](https://qgroundcontrol.com/downloads/) |
| Git LFS            | Large binaries / logs                | `sudo apt install git-lfs`                 |

---

## 3. Repository Layout

```text
quadcopter/
|-- README.md                    # Project overview
|-- INSTRUCTIONS.md              # This document
|-- hardware/                    # Mechanical / airframe docs
|-- electronics/                 # Wiring, BOM, system architecture
`-- src/                         # ROS 2 colcon workspace
    |-- quadcopter_bringup/      # Autonomy bringup package (Python)
    |-- px4_ros_com/             # PX4 <-> ROS 2 bridge + examples
    |-- px4_msgs/                # PX4 uORB message/service definitions
    |-- build/                   # colcon build output (gitignored)
    |-- install/                 # colcon install prefix (gitignored)
    `-- log/                     # colcon logs (gitignored)
```

> **Note:** `src/build`, `src/install`, and `src/log` are generated by colcon
> and are gitignored. Never commit them.

---

## 4. Workspace Setup

### 4.1 Clone the Repository

```bash
git clone https://github.com/the-robotronics-club/quadcopter
cd quadcopter
```

`src/` is a colcon workspace root: all `colcon` commands below are run from
inside `src/`.

### 4.2 Source ROS 2

```bash
source /opt/ros/humble/setup.bash
```

### 4.3 Resolve Dependencies

```bash
cd src
rosdep install --from-paths . --ignore-src -r -y
```

> `px4_msgs` and `px4_ros_com` are vendored from upstream PX4. Keep them in
> sync with the PX4 firmware version you intend to use (see their READMEs).

---

## 5. Building the Workspace

```bash
cd src
source /opt/ros/humble/setup.bash

colcon build --symlink-install
```

### 5.1 Selective Builds

```bash
# Build only the autonomy bringup package
colcon build --packages-select quadcopter_bringup

# Build a subset
colcon build --packages-select px4_msgs px4_ros_com

# Release build for performance (Jetson deployment)
colcon build --cmake-args -DCMAKE_BUILD_TYPE=RELWITHDEBINFO --symlink-install
```

### 5.2 Rebuild After Interface Changes

When `px4_msgs` message definitions change, rebuild the interface package
first, then everything that depends on it:

```bash
colcon build --packages-select px4_msgs --cmake-clean-cache
colcon build --symlink-install
```

### 5.3 Source the Workspace

```bash
source install/setup.bash
```

Recommended `~/.bashrc` additions:

```bash
source /opt/ros/humble/setup.bash
source ~/path/to/quadcopter/src/install/setup.bash
```

### 5.4 Cleaning

```bash
colcon build --cmake-clean-cache   # rebuild CMake cache only
rm -rf build install log           # full clean
```

---

## 6. Running the System

### 6.1 MicroXRCE-DDS Agent (Required for PX4 <-> ROS 2)

PX4 and ROS 2 communicate over **uXRCE-DDS** (micro-ROS). The flight controller
runs a micro XRCE-DDS *client* and the companion computer runs the
**MicroXRCE-DDS Agent**, which exposes PX4 uORB topics on the ROS 2 graph as
`/fmu/*` topics. Nothing works until this agent is running.

#### 6.1.1 Install

```bash
# Option A - prebuilt Debian package (simplest)
sudo apt install ros-humble-micro-ros-agent

# Option B - build from source (latest, useful on Jetson)
git clone https://github.com/eProsima/Micro-XRCE-DDS-Agent.git
cd Micro-XRCE-DDS-Agent && mkdir build && cd build
cmake ..
make
sudo make install
sudo ldconfig
```

Both produce the `MicroXRCEAgent` executable (and, if installed via the ROS
package, the `micro-ros-agent` alias).

#### 6.1.2 Transports

| Transport | Use case                   | Command                                              |
| --------- | -------------------------- | ---------------------------------------------------- |
| UDP       | SITL simulation (loopback) | `MicroXRCEAgent udp4 -p 8888 -v`                     |
| Serial    | Pixhawk over UART (TELEM2) | `MicroXRCEAgent serial --dev /dev/ttyTHS1 -b 921600` |
| Ethernet  | Pixhawk on the network     | `MicroXRCEAgent udp4 -p 2019 -r 2020`                |

#### 6.1.3 PX4-side configuration

- **Hardware (Pixhawk):** set `UXRCE_DDS_CFG` to **TELEM2** and
  `SER_TEL2_BAUD` to `921600` in QGroundControl, then reboot the flight
  controller.
- **SITL:** the PX4 SITL firmware starts its own uXRCE-DDS client on UDP port
  `8888`, so the agent must listen on that port. No extra config required.

#### 6.1.4 Verify

```bash
ros2 daemon stop && ros2 topic list | grep -E 'fmu|vehicle'
```

If no `/fmu/*` topics appear, the agent is not running, the client is not
connected, or the transport/baud settings are wrong.

### 6.2 Inspect the ROS 2 Graph

```bash
ros2 node list
ros2 topic list
ros2 topic echo /fmu/out/vehicle_attitude
```

### 6.3 Example Nodes (`px4_ros_com`)

```bash
# Sensor data listeners
ros2 run px4_ros_com sensor_combined_listener
ros2 run px4_ros_com vehicle_gps_position_listener

# Offboard control example (CAUTION: engages motors)
ros2 run px4_ros_com offboard_control
```

Or via launch files:

```bash
ros2 launch px4_ros_com sensor_combined_listener.launch.py
```

### 6.4 Bringup - Simulation (SITL)

```bash
# Terminal 1 - MicroXRCE-DDS agent over UDP loopback
MicroXRCEAgent udp4 -p 8888 -v

# Terminal 2 - PX4 SITL + Gazebo (from the PX4-Autopilot source directory)
make px4_sitl gazebo

# Terminal 3 - ROS 2 node
cd src && source install/setup.bash
ros2 run quadcopter_bringup teleop
```

### 6.5 Bringup - Hardware (Real Robot)

1. Power the airframe; confirm the Pixhawk boots and the Jetson links over
   UART.
2. Set `UXRCE_DDS_CFG` = TELEM2 and `SER_TEL2_BAUD` = 921600 in
   QGroundControl and reboot the flight controller.
3. Verify MAVLink telemetry in QGroundControl.
4. Start the agent and the ROS 2 node:

```bash
MicroXRCEAgent serial --dev /dev/ttyTHS1 -b 921600

cd src && source install/setup.bash
ros2 run quadcopter_bringup teleop
```

### 6.6 Controlling the Quadcopter (Keyboard Teleop)

`quadcopter_bringup` ships a keyboard teleop node that drives the PX4
velocity setpoints in Offboard mode over uXRCE-DDS:

```bash
ros2 run quadcopter_bringup teleop
```

| Key      | Action                    |
| -------- | ------------------------- |
| `w`      | Move forward (+X)         |
| `s`      | Move back (-X)            |
| `a`      | Move left (+Y)            |
| `d`      | Move right (-Y)           |
| `r`      | Move up (-Z, NED)         |
| `f`      | Move down (+Z, NED)       |
| `x`      | Stop / hover in place     |
| `q`      | Increase max speed by 10% |
| `z`      | Decrease max speed by 10% |
| `Ctrl+C` | Exit safely               |

The node publishes `OffboardControlMode` and `TrajectorySetpoint` at 20 Hz,
switches PX4 to **Offboard** mode after ~15 setpoints, and sends the **arm**
command after ~30. Always keep the RC transmitter in an override-capable mode
and test in SITL before hardware.

### 6.7 Common Runtime Checks

```bash
# Lifecycle state (if the node is lifecycle-managed)
ros2 lifecycle get <node_name>

# CPU / temperature on the Jetson
sudo jetson_clocks
tegrastats

# Verbose logging
ros2 launch <pkg> <file> --log-level debug
```

---

## 7. Testing

Run the full workspace tests:

```bash
cd src
source /opt/ros/humble/setup.bash
colcon test
colcon test-result --verbose
```

### 7.1 Targeted Tests

```bash
# Lint/style for the bringup package (flake8, pep257, copyright)
colcon test --packages-select quadcopter_bringup
colcon test-result --verbose

# px4_ros_com unit + pipeline tests
colcon test --packages-select px4_ros_com
```

Lint only, without running tests:

```bash
colcon test --packages-select quadcopter_bringup --ctest-args -L linter
```

---

## 8. Development Workflow

### 8.1 Conventions

- **ROS 2 (Humble):** `ament_python` / `ament_cmake` packages with
  `package.xml` (format 3).
- **Python:** PEP 8, enforced by `flake8` / `pep257` test suites. Do not
  disable these checks.
- **C++:** C++17 (see `.vscode/c_cpp_properties.json`), enforced by
  `ament_lint_auto`.
- **Messages:** add any new uORB-facing types to `px4_msgs`; keep definitions
  aligned with PX4.

### 8.2 Adding a New Node

1. Create the node under `src/quadcopter_bringup/quadcopter_bringup/`.
2. Register executables in `setup.py` (`entry_points.console_scripts`).
3. Add launch files under `src/quadcopter_bringup/launch/` and list them in the
   `data_files` of `setup.py`.
4. Rebuild and test:

```bash
cd src && colcon build --packages-select quadcopter_bringup --symlink-install
colcon test --packages-select quadcopter_bringup
```

### 8.3 Code Review Checklist

- [ ] Dependencies declared in `package.xml`.
- [ ] `flake8` and `pep257` clean.
- [ ] No hardcoded paths/ports - use launch parameters.
- [ ] Topics/services namespaced (e.g. `/fmu/in/`, `/perception/`).
- [ ] Graceful shutdown on `SIGINT`; no orphaned agents.
- [ ] Log messages include node name and severity level.

### 8.4 Git Guidelines

- Branch per feature: `git checkout -b feature/<name>`.
- Conventional commit messages (e.g. `feat:`, `fix:`).
- Rebase onto `main` before opening a Pull Request.
- Never commit `build/`, `install/`, `log/`, or `.vscode/` artifacts.

---

## 9. Troubleshooting

| Symptom                              | Likely Cause                            | Resolution                                                        |
| ------------------------------------ | --------------------------------------- | ----------------------------------------------------------------- |
| No `/fmu/*` topics                   | MicroXRCE-Agent not running             | Start the agent (Section 6.1); verify with `ros2 topic list`.      |
| Agent starts but no data in SITL     | Wrong UDP port                         | PX4 SITL client uses port `8888`; run `MicroXRCEAgent udp4 -p 8888`. |
| Agent disconnects on hardware        | Wrong port / baud rate                  | Check TELEM2 wiring, set `-b 921600`, and confirm `UXRCE_DDS_CFG` = TELEM2 in QGC. |
| `ros2 run` says package not found    | Workspace not sourced                   | `source install/setup.bash` after `colcon build`.                  |
| `ros2 run quadcopter_bringup teleop` not found | Old build / entry point not installed | `colcon build --packages-select quadcopter_bringup --symlink-install`. |
| Lint test failures                   | Style violations                        | Run `colcon test --packages-select quadcopter_bringup` and fix reported issues. |
| SITL model does not start            | Gazebo / PX4 environment not sourced    | Source the PX4 `Tools/setup_gazebo.bash` and `~/PX4-Autopilot` env. |
| Teleop does not arm                  | Not in Offboard / safety checks fail    | Confirm mode switch + arm log lines; check QGC arming checks; keep RC override available. |
| High CPU/temperature on Jetson       | Clock throttling                        | `sudo jetson_clocks`; cap detection frame rate.                    |

---

## 10. Safety & Operational Guidelines

- Always operate with propellers removed during bringup, calibration, and
  offboard development.
- Arm the vehicle only after the flight controller passes preflight checks in
  QGroundControl and an RC failsafe is configured.
- Keep the RC transmitter in a **manual-override-capable** flight mode and test
  failsafes (RTL, low battery) in SITL before hardware.
- Run offboard tests in SITL first; verify the kill-switch / disarming behavior
  before real-robot trials.
- Fly within line of sight, in a cleared area, with a spotter, and never over
  people.
- Secure the battery and confirm secure propeller retention before flight.

---

## References

- [README.md](README.md) - Project overview
- [hardware/README.md](hardware/README.md) - Airframe specs
- [electronics/README.md](electronics/README.md) - System architecture & data flow
- [PX4 ROS 2 User Guide](https://docs.px4.io/main/en/ros/ros2_comm.html)
- [PX4 uXRCE-DDS](https://docs.px4.io/main/en/middleware/uxrce_dds.html)